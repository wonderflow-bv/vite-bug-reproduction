import { useState } from 'react'
import logo from 'vite-bug-reproduction-asset/logo'
import sprite from 'vite-bug-reproduction-asset/sprite'
import './App.css'

import "@wonderflow/react-components/core.css";
import "@wonderflow/themes";

// Comment and decomment this component to see the issue while serving the app
import { Icon } from '@wonderflow/react-components';

function App() {
  const [count, setCount] = useState(0)

  return (
    <div className="App">
      <header className="App-header">
        <img src={logo} className="App-logo" alt="logo" />
        <p>Hello Vite + React! <svg><use href={`${sprite}#solid/accessibility`} /></svg></p>
        <p>
          <button type="button" onClick={() => setCount((count) => count + 1)}>
            count is: {count}
          </button>
        </p>
        <p>
          Edit <code>App.jsx</code> and save to test HMR updates.
        </p>
        <p>
          <a
            className="App-link"
            href="https://reactjs.org"
            target="_blank"
            rel="noopener noreferrer"
          >
            Learn React
          </a>
          {' | '}
          <a
            className="App-link"
            href="https://vitejs.dev/guide/features.html"
            target="_blank"
            rel="noopener noreferrer"
          >
            Vite Docs
          </a>
        </p>
      </header>
    </div>
  )
}

export default App
